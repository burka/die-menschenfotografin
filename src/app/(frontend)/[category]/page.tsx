// Category pages are now rendered by AppShell via ContentLayer
// This file is kept for Next.js routing compatibility
// The NavigationContext initializes from the URL pathname

export default function CategoryPage(): null {
  // Content is rendered by AppShell's ContentLayer based on navigation state
  return null
}
